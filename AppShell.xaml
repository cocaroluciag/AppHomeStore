<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:AppHomeStore.Views"
       x:Class="AppHomeStore.AppShell">

    <ShellContent ContentTemplate="{DataTemplate views:LoginPage}" />

    <ShellContent Title="Usuarios" ContentTemplate="{DataTemplate views:UsuarioListaPage}" />

    <ShellContent Route="UsuarioCrearPage" ContentTemplate="{DataTemplate views:UsuarioCrearPage}" />
</Shell>
